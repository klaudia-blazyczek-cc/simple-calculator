import styled from 'styled-components';

import CalculationsPanel from './CalculationsPanel.component';

const StyledCalculationsPanel = styled(CalculationsPanel)`
	padding: 10px 15px;
	height: 100px;
	display: flex;
	flex-direction: column;
	justify-content: center;
	align-items: flex-end;
	font-size: 2.2rem;
	color: #fff;
`;

export default StyledCalculationsPanel;
